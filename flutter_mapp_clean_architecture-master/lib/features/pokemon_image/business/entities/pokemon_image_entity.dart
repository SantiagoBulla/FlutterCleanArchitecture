// * This will be the structure to the stores the path image that we could use to render the pokemon image
class PokemonImageEntity {
  final String path;
  const PokemonImageEntity({
    required this.path,
  });
}
